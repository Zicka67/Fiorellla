<nav>

    <div class="nav-left">
        <a href="{{ path('app_home') }}"><span class="italian-flag green">Fio</span><span class="italian-flag white">rell</span><span class="italian-flag red">la</span></a>
    </div>

    <div class="nav-center">
        <a href="{{ path('app_home') }}">Accueil</a>
        <a href="#">Lien1</a>
        <a href="#">Lien2</a>
        <a href="#">Contact</a>
    </div>
    
    {# <div class="nav-right">
        {% if not app.user %}
            <a href="{{ path('app_register') }}" id="register">Inscription</a>
        {% endif %}
        {% if app.user %}
            <a href="{{ path('app_logout') }}" id="logout">Déconnexion</a>
        {% else %}
            <a href="{{ path('app_login') }}" id="login">Connexion</a>
        {% endif %}
    </div> #}

</nav>
